/**
 * @modernrelay/orbit-engine-cosmos — CosmosEngine, the first GraphEngine
 * implementation. The only package importing
 * @cosmos.gl/graph; cosmos is loaded lazily at mount time.
 */
export { CosmosEngine } from './CosmosEngine';
export type { CosmosEngineOptions } from './CosmosEngine';
